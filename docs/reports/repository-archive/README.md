# Repository documentation archive

Files in this directory were **moved from the repository root** during the Phase 40 documentation consolidation. They include:

- historical phase reports (`PHASE*.md`, early integration notes),
- design and capability models (`*_MODEL.md`),
- audits, handoffs, and one-off analyses.

Nothing here was deleted: paths changed to keep the **root minimal** and `docs/` navigable.

Prefer current truth in:

- `docs/architecture/` — maintained architecture views
- `docs/phases/phase-31.md` … `phase-40.md` — phase-scoped implementation records
- `backend/python/brain/runtime/` — runtime source

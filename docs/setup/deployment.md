# Deployment

## Frontend (Cloudflare Pages)

See [../deploy/CLOUDFLARE_PAGES_FRONTEND.md](../deploy/CLOUDFLARE_PAGES_FRONTEND.md).

## Containers

Docker/Compose assets (when present) should mirror CI parity; validate against the same health checks used post-deploy in automation.

## References

- `CHANGELOG.md` — release-visible changes
- Repository CI workflows under `.github/workflows/`

// Generated by scripts/package-queryengine.mjs
// Source: vendor/src-brain-upstream/queryEngineRunnerAdapter.js
// This file packages the selected upstream QueryEngine subset and
// falls back to the active authority for execution-heavy requests.
function normalizeText(value) {
  return String(value || '')
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .toLowerCase()
    .trim();
}

function getUserMemory(memoryContext) {
  const user = memoryContext?.user;
  if (!user || typeof user !== 'object') {
    return { nome: '', preferencias: [] };
  }

  return {
    nome: typeof user.nome === 'string' ? user.nome.trim() : '',
    preferencias: Array.isArray(user.preferencias)
      ? user.preferencias.map(item => String(item).trim()).filter(Boolean)
      : [],
  };
}

function getRecentUserMessages(history) {
  if (!Array.isArray(history)) {
    return [];
  }

  return history
    .filter(item => item?.role === 'user' && typeof item?.content === 'string')
    .map(item => item.content.trim())
    .filter(Boolean)
    .slice(-6);
}

function buildContextSummary(history, summary, session) {
  if (summary && String(summary).trim()) {
    return String(summary).trim();
  }

  const sessionSummary = session?.summary;
  if (typeof sessionSummary === 'string' && sessionSummary.trim()) {
    return sessionSummary.trim();
  }

  const recent = getRecentUserMessages(history);
  if (recent.length === 0) {
    return 'Sem contexto anterior relevante.';
  }

  return recent.join(' | ');
}

function detectIntent(message, recentHistory) {
  const msg = normalizeText(message);
  const historyText = normalizeText(recentHistory.join(' '));

  const isDecision =
    msg.includes('devo') ||
    msg.includes(' ou ') ||
    msg.includes('qual e melhor') ||
    msg.includes('o que eu faco') ||
    msg.includes('o que fazer') ||
    msg.includes('vale a pena') ||
    msg.includes('melhor opcao');

  if (isDecision) {
    return 'decision';
  }

  if (
    msg.includes('ganhar dinheiro') ||
    msg.includes('negocio') ||
    msg.includes('dinheiro') ||
    msg.includes('renda') ||
    msg.includes('vender online')
  ) {
    return 'dinheiro';
  }

  if (
    msg.includes('quero aprender') ||
    msg.includes('por onde comeco') ||
    msg.includes('programacao') ||
    msg.includes('estudar') ||
    historyText.includes('quero aprender')
  ) {
    return 'aprendizado';
  }

  if (
    msg.includes('como melhorar') ||
    msg.includes('conselho') ||
    msg.includes('dica')
  ) {
    return 'conselho';
  }

  if (
    msg.includes('o que e') ||
    msg.includes('explique') ||
    msg.includes('por que') ||
    msg.includes('porque') ||
    msg.includes('como funciona')
  ) {
    return 'explicacao';
  }

  if (
    msg.includes('quem e voce') ||
    msg.includes('como voce responde') ||
    msg.includes('como voce funciona')
  ) {
    return 'pessoal';
  }

  return 'conversa';
}

function buildCapabilityMap(capabilities) {
  if (!Array.isArray(capabilities)) {
    return {};
  }

  const entries = capabilities
    .filter(item => item && typeof item === 'object' && typeof item.name === 'string')
    .map(item => [item.name, item]);

  return Object.fromEntries(entries);
}

function think(message, memoryContext, history, summary, capabilities, session) {
  const userMemory = getUserMemory(memoryContext);
  const recentHistory = getRecentUserMessages(history);
  const capabilityMap = buildCapabilityMap(capabilities);
  const intent = detectIntent(message, recentHistory);

  return {
    intent,
    userName: userMemory.nome,
    preferences: userMemory.preferencias,
    contextSummary: buildContextSummary(history, summary, session),
    message,
    recentHistory,
    capabilityMap,
    availableCapabilities: Object.keys(capabilityMap),
    session,
    memoryEvidence: {
      hasName: Boolean(userMemory.nome),
      hasPreferences: userMemory.preferencias.length > 0,
      previousTurns: Array.isArray(session?.history) ? session.history.length : 0,
    },
    delegates:
      intent === 'dinheiro'
        ? ['planner', 'advisor']
        : intent === 'aprendizado'
          ? ['teacher']
          : ['assistant'],
  };
}

function decide(thought) {
  const canPlan = thought.availableCapabilities.includes('create_plan');
  const canAdvise = thought.availableCapabilities.includes('give_advice');
  const canIdeate = thought.availableCapabilities.includes('generate_idea');

  switch (thought.intent) {
    case 'decision':
      return { strategy: canAdvise ? 'decision_help' : 'contextual_conversation', confidence: 0.92 };
    case 'dinheiro':
      return { strategy: canIdeate ? 'business_plan' : 'contextual_conversation', confidence: 0.9 };
    case 'conselho':
      return { strategy: canAdvise ? 'practical_advice' : 'contextual_conversation', confidence: 0.86 };
    case 'aprendizado':
      return { strategy: canPlan ? 'learning_path' : 'structured_explanation', confidence: 0.89 };
    case 'pessoal':
      return { strategy: 'identity_reply', confidence: 0.95 };
    case 'explicacao':
      return { strategy: 'structured_explanation', confidence: 0.83 };
    default:
      return { strategy: 'contextual_conversation', confidence: 0.74 };
  }
}

function hasPreference(preferences, keyword) {
  return preferences.some(item => normalizeText(item).includes(keyword));
}

function buildExecutionPlan(thought, decision) {
  switch (decision.strategy) {
    case 'business_plan':
      return ['avaliar contexto', 'selecionar oportunidade', 'montar proposta inicial'];
    case 'learning_path':
      return ['diagnosticar nivel atual', 'ordenar fundamentos', 'propor pratica real'];
    case 'decision_help':
    case 'practical_advice':
      return ['entender tradeoff', 'avaliar energia e risco', 'recomendar proximo passo'];
    case 'structured_explanation':
      return ['definir conceito', 'explicar impacto', 'dar exemplo util'];
    default:
      return ['entender objetivo', 'responder com utilidade'];
  }
}

function act(decision, thought) {
  const msg = normalizeText(thought.message);
  const likesTech =
    hasPreference(thought.preferences, 'tecnologia') ||
    hasPreference(thought.preferences, 'programacao');
  const executionPlan = buildExecutionPlan(thought, decision);

  switch (decision.strategy) {
    case 'business_plan': {
      if (msg.includes('ideia de negocio')) {
        return {
          executionPlan,
          payload: likesTech
            ? [
                'Validar um servico de automacao simples para negocios locais.',
                'Oferecer implantacao rapida com mensalidade leve.',
                'Fechar os primeiros clientes antes de ampliar o escopo.',
              ]
            : [
                'Escolher um problema simples e frequente de um publico especifico.',
                'Montar uma oferta pequena e facil de vender.',
                'Testar rapido com clientes reais antes de investir mais.',
              ],
        };
      }

      if (msg.includes('ganhar dinheiro online')) {
        return {
          executionPlan,
          payload: [
            'Escolha um servico simples que voce consiga entregar bem.',
            'Monte uma oferta clara com exemplo de resultado.',
            'Busque clientes todos os dias em vez de esperar a oportunidade perfeita.',
          ],
        };
      }

      return {
        executionPlan,
        payload: [
          'Combine habilidade vendavel com execucao constante.',
          'Crie uma oferta simples e resolva um problema real.',
          'Busque validacao rapida antes de pensar em escalar.',
        ],
      };
    }

    case 'decision_help': {
      if (msg.includes('estudar') && msg.includes('descansar')) {
        return {
          executionPlan,
          analysis: [
            'Se sua energia mental estiver baixa, descansar primeiro pode melhorar sua qualidade de estudo.',
            'Se ainda houver foco, um bloco curto de estudo gera progresso sem te esgotar.',
          ],
          recommendation:
            'Se voce estiver cansado, descansar agora pode ser melhor. Se ainda tiver energia, estude por 25 minutos com objetivo pequeno e reavalie depois.',
        };
      }

      return {
        executionPlan,
        analysis: [
          'A melhor escolha depende do seu estado atual e do impacto imediato de cada opcao.',
          'Em geral, vale priorizar a opcao que cria progresso sem gerar desgaste desnecessario.',
        ],
        recommendation:
          'Compare as opcoes pelo custo de energia e pelo ganho pratico no curto prazo.',
      };
    }

    case 'practical_advice':
      return {
        executionPlan,
        analysis: [
          'O melhor conselho quase sempre nasce de simplificar o proximo passo.',
          'Quando voce reduz o escopo, aumenta a chance de agir de verdade.',
        ],
        recommendation:
          'Escolha uma acao pequena, conclua hoje e use isso como base para a proxima decisao.',
      };

    case 'learning_path': {
      if (msg.includes('programacao')) {
        return {
          executionPlan,
          steps: [
            'Aprenda logica de programacao, variaveis, condicionais e lacos.',
            'Pratique com JavaScript ou Python em projetos pequenos.',
            'Avance para APIs, Git e um projeto real simples.',
          ],
        };
      }

      return {
        executionPlan,
        steps: [
          'Comece pelos fundamentos antes de buscar velocidade.',
          'Pratique no mesmo dia com exercicios curtos.',
          'Transforme o estudo em um projeto pequeno e concluido.',
        ],
      };
    }

    case 'identity_reply':
      return {
        executionPlan,
        identity:
          'Sou uma IA orientada a contexto, memoria, decisao pratica e aprendizado progressivo. Meu trabalho e entender o que voce quer e te devolver algo util.',
      };

    case 'structured_explanation':
      return {
        executionPlan,
        explanation: [
          'Definir com clareza o conceito central.',
          'Mostrar por que isso importa na pratica.',
          'Traduzir a ideia em exemplo simples e acionavel.',
        ],
      };

    default:
      return {
        executionPlan,
        conversation:
          thought.recentHistory.length > 0
            ? 'Vou continuar a partir do contexto recente e focar no proximo passo mais util.'
            : 'Vou responder de forma direta e util com base no que voce perguntou.',
      };
  }
}

function updateMemory(actionResult, thought) {
  return {
    memorySignal: {
      intent: thought.intent,
      usedName: Boolean(thought.userName),
      usedPreferences: thought.preferences.length > 0,
      executionPlan: actionResult.executionPlan || [],
      delegates: thought.delegates,
    },
  };
}

function maybeUseName(thought) {
  return thought.userName ? `${thought.userName}, ` : '';
}

function respond(actionResult, thought, memoryResult, decision) {
  const opener = maybeUseName(thought);
  const planHint =
    Array.isArray(memoryResult?.memorySignal?.executionPlan) &&
    memoryResult.memorySignal.executionPlan.length > 0
      ? `\nPlano interno: ${memoryResult.memorySignal.executionPlan.join(' -> ')}`
      : '';

  switch (thought.intent) {
    case 'decision':
      return {
        response: `${opener}${actionResult.analysis[0]} ${actionResult.analysis[1]} ${actionResult.recommendation}`,
        confidence: decision.confidence,
        memory: memoryResult.memorySignal,
      };
    case 'dinheiro':
      return {
        response: `${opener}eu seguiria um caminho realista:\n1. ${actionResult.payload[0]}\n2. ${actionResult.payload[1]}\n3. ${actionResult.payload[2]}`,
        confidence: decision.confidence,
        memory: memoryResult.memorySignal,
      };
    case 'conselho':
      return {
        response: `${opener}${actionResult.analysis[0]} ${actionResult.analysis[1]} ${actionResult.recommendation}`,
        confidence: decision.confidence,
        memory: memoryResult.memorySignal,
      };
    case 'aprendizado':
      return {
        response: `${opener}o melhor caminho agora e este:\n1. ${actionResult.steps[0]}\n2. ${actionResult.steps[1]}\n3. ${actionResult.steps[2]}`,
        confidence: decision.confidence,
        memory: memoryResult.memorySignal,
      };
    case 'pessoal':
      return {
        response: thought.userName
          ? `${actionResult.identity} Tambem levo em conta o que voce ja me contou, ${thought.userName}.`
          : actionResult.identity,
        confidence: decision.confidence,
        memory: memoryResult.memorySignal,
      };
    case 'explicacao':
      return {
        response: `${opener}eu pensaria assim: ${actionResult.explanation[0]}, depois ${actionResult.explanation[1]} e por fim ${actionResult.explanation[2]}.`,
        confidence: decision.confidence,
        memory: memoryResult.memorySignal,
      };
    default:
      return {
        response: `${opener}${actionResult.conversation} Se quiser, me diga o objetivo principal e eu organizo a resposta com mais precisao.`,
        confidence: decision.confidence,
        memory: memoryResult.memorySignal,
      };
  }
}

async function runPackagedUpstreamQueryEngine({
  message,
  memoryContext,
  history,
  summary,
  capabilities,
  session,
}) {
  const thought = think(message, memoryContext, history, summary, capabilities, session);
  const decision = decide(thought);
  const actionResult = act(decision, thought);
  const memoryResult = updateMemory(actionResult, thought);
  const response = respond(actionResult, thought, memoryResult, decision);
  return response;
}

import fusionBrainModule from '../core/brain/fusionBrain.js'
import fs from 'node:fs'
import path from 'node:path'
import { fileURLToPath } from 'node:url'

const activeRunQueryEngine =
  typeof fusionBrainModule?.runQueryEngine === 'function'
    ? fusionBrainModule.runQueryEngine
    : typeof fusionBrainModule?.default?.runQueryEngine === 'function'
      ? fusionBrainModule.default.runQueryEngine
      : null

const PHASE27_PROMOTED_SCENARIO = 'executor_bridge_light_request'
const PHASE27_PROMOTION_PHASE = '27'
const PHASE27_ROLLBACK_REASON = 'packaged_import_failed_threshold_exceeded'
const PHASE27_IMPORT_FAILURE_THRESHOLD = 2
const ENGINE_ADOPTION_PATH = path.resolve(
  path.dirname(fileURLToPath(import.meta.url)),
  '..',
  '.logs',
  'fusion-runtime',
  'engine_adoption.json',
)
const CONTROL_SESSION_KEYS = [
  'executor_bridge',
  'runtime_mode',
  'milestone_plan',
  'repository_analysis',
  'task_id',
  'run_id',
  'checkpoint_id',
  'resume_from',
]
const PROMOTION_BLOCKING_SESSION_KEYS = CONTROL_SESSION_KEYS.filter(key => key !== 'executor_bridge')
const ENGINEERING_SIGNALS = [
  'package.json',
  'cargo',
  'npm',
  'repositorio',
  'repository',
  'arquivo',
  'file',
  'teste',
  'tests',
  'corrija',
  'corrigir',
  'fix',
  'bug',
  'erro',
  'error',
  'codigo',
  'code',
  'commit',
  'branch',
  'worktree',
  'leia ',
  'analyze the repository',
  'analise o repositorio',
]

function normalizeRoutingText(value) {
  return String(value || '')
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .toLowerCase()
    .trim()
}

function hasAnySessionKey(session, keys) {
  return keys.some(key => Object.prototype.hasOwnProperty.call(session || {}, key))
}

function isHeavyEngineeringRequest(payload) {
  const message = normalizeRoutingText(payload?.message)
  if (!message) {
    return false
  }

  return ENGINEERING_SIGNALS.some(signal => message.includes(signal))
}

function getPromotedScenario(payload) {
  const session = payload?.session || {}
  if (!Object.prototype.hasOwnProperty.call(session, 'executor_bridge')) {
    return ''
  }

  if (hasAnySessionKey(session, PROMOTION_BLOCKING_SESSION_KEYS)) {
    return ''
  }

  if (isHeavyEngineeringRequest(payload)) {
    return ''
  }

  return PHASE27_PROMOTED_SCENARIO
}

function shouldUseActiveAuthority(payload) {
  if (getPromotedScenario(payload)) {
    return false
  }

  const session = payload?.session || {}
  if (hasAnySessionKey(session, CONTROL_SESSION_KEYS)) {
    return true
  }

  return isHeavyEngineeringRequest(payload)
}

function determineFallbackReason(payload) {
  const session = payload?.session || {}
  if (hasAnySessionKey(session, CONTROL_SESSION_KEYS)) {
    return 'fallback_policy_triggered'
  }

  if (isHeavyEngineeringRequest(payload)) {
    return 'heavy_execution_request'
  }

  return 'fallback_policy_triggered'
}

function readPackagedImportFailureCount() {
  try {
    const payload = JSON.parse(fs.readFileSync(ENGINE_ADOPTION_PATH, 'utf8'))
    const fallbackByReason = payload?.engine_counters?.fallback_by_reason
    if (!fallbackByReason || typeof fallbackByReason !== 'object') {
      return 0
    }
    return Number(fallbackByReason.packaged_import_failed || 0)
  } catch {
    return 0
  }
}

function shouldRollbackPromotedScenario(payload, promotedScenario) {
  if (!promotedScenario) {
    return false
  }

  return readPackagedImportFailureCount() > PHASE27_IMPORT_FAILURE_THRESHOLD
}

function withEngineMetadata(result, engineMode, engineReason, extraMetadata = {}) {
  const normalized =
    result && typeof result === 'object' && !Array.isArray(result)
      ? { ...result }
      : { response: String(result || '').trim() }

  const metadata =
    normalized.metadata && typeof normalized.metadata === 'object' && !Array.isArray(normalized.metadata)
      ? { ...normalized.metadata }
      : {}

  normalized.metadata = {
    ...metadata,
    ...extraMetadata,
    engine_mode: engineMode,
    engine_reason: engineReason,
  }

  return normalized
}

export async function runQueryEngine(payload) {
  const promotedScenario = getPromotedScenario(payload)
  if (promotedScenario && shouldRollbackPromotedScenario(payload, promotedScenario) && typeof activeRunQueryEngine === 'function') {
    return withEngineMetadata(
      await activeRunQueryEngine(payload),
      'authority_fallback',
      'fallback_policy_triggered',
      {
        promoted_scenario: promotedScenario,
        promotion_phase: PHASE27_PROMOTION_PHASE,
        promotion_rollback_reason: PHASE27_ROLLBACK_REASON,
      },
    )
  }

  if (shouldUseActiveAuthority(payload) && typeof activeRunQueryEngine === 'function') {
    return withEngineMetadata(
      await activeRunQueryEngine(payload),
      'authority_fallback',
      determineFallbackReason(payload),
    )
  }

  return withEngineMetadata(
    await runPackagedUpstreamQueryEngine(payload),
    'packaged_upstream',
    'dist_candidate_selected',
    promotedScenario
      ? {
          promoted_scenario: promotedScenario,
          promotion_phase: PHASE27_PROMOTION_PHASE,
        }
      : {},
  )
}

export { runPackagedUpstreamQueryEngine }
export default runQueryEngine

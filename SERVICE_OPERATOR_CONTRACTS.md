# Service Operator Contracts

## Complex task submission
- user_id
- session_id
- message

## Inspection contracts
- inspect plan hierarchy by run id
- inspect run status by run id
- inspect checkpoint by run id
- inspect reflection summary by run id
- inspect execution-learning memory entries

## Operator-safe model
- Inspection is read-only.
- Policy and hierarchy are exposed as structured objects.
- Internal contracts are ready to be mapped into a future API surface.
